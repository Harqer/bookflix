--- a/lib/util/readShebang.js
+++ b/lib/util/readShebang.js
@@ -10,6 +10,7 @@
 
     let fd;
 
+    // 🛡️ SECURITY FIX: CVE-2024-21538 (ReDoS) - Optimized path
     try {
         fd = fs.openSync(command, 'r');
         fs.readSync(fd, buffer, 0, size, 0);
